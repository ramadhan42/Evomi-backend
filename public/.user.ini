; Hostinger / shared PHP limits for product image uploads (per file up to 40MB).
; Multiple images in one request need a larger POST body.
upload_max_filesize = 40M
post_max_size = 180M
memory_limit = 256M
max_execution_time = 300
max_input_time = 300
